import { StackScreenProps } from "@react-navigation/stack";
import { StackNavigatorParams } from "../navigation/stackNavigator";

export type HomeProps = StackScreenProps<StackNavigatorParams, "Home">;
export type FinishProps = StackScreenProps<StackNavigatorParams, "Finish">;
